---
description: Check bd CLI and pi package versions
---
Check the installed Beads CLI version and the pi package version.

Guidelines:
- Run `bd version` if `bd` is available.
- Read `package.json` from this installed package if you need the pi package version.
- Report clearly whether `bd` is installed, what version it is, and whether the project appears initialized.
- If `bd` is missing, include install guidance.
