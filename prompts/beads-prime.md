---
description: Show bd prime workflow context
---
Run `bd prime` and show the workflow context it produces.

Guidelines:
- If `bd prime` returns useful context, present it cleanly and explain how it affects the current workflow.
- If it returns nothing, explain that there may be no extra Beads context yet for this project.
- If Beads is not initialized, mention `/beads:init`.
